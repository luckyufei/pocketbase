/**
 * TaggedHook — 标签过滤钩子（独立文件导出）
 * 三层代理：Model → Record/Collection
 */

import { Hook, TaggedHook, TaggedHookView, type Handler } from "./hook";

export { Hook, TaggedHook, TaggedHookView, type Handler };
