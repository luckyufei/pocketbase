# Implementation Plan: WebUI New Collection 功能 1:1 对齐

**Branch**: `022-webui-new-collection-alignment` | **Date**: 2026-02-03 | **Spec**: [spec.md](./spec.md)

## Summary

将 WebUI (React) 版本的 New Collection 功能与 UI (Svelte) 版本完全对齐，包括：
1. 侧边栏底部新建入口
2. Secret 字段类型支持
3. 默认时间戳字段
4. 变更确认面板

## Technical Context

**Language/Version**: TypeScript + React 18  
**Primary Dependencies**: 
- `@dnd-kit/core` (拖拽排序)
- `shadcn/ui` (UI 组件)
- `lucide-react` (图标)

**Testing**: Vitest + React Testing Library  
**Target Platform**: Web Browser  
**Project Type**: React SPA (Admin Dashboard)  
**Code Style**: TDD (红灯 → 绿灯 → 重构)

## Constitution Check

| Principle | Status | Notes |
|-----------|--------|-------|
| 1:1 功能对齐 | ✅ PASS | 与 UI (Svelte) 版本功能一致 |
| 现有架构兼容 | ✅ PASS | 遵循现有 WebUI 代码风格 |
| 测试覆盖 | ✅ PASS | TDD 流程，覆盖率 ≥ 80% |
| UI 设计规范 | ✅ PASS | 遵循项目 Apple-style 设计规范 |

## Project Structure

### Documentation

```text
specs/022-webui-new-collection-alignment/
├── spec.md              # Feature specification
├── plan.md              # This file
└── tasks.md             # Implementation tasks
```

### Source Code Changes

```text
webui/src/features/collections/components/
├── Sidebar.tsx                        # [MODIFY] 添加底部新建按钮
├── UpsertPanel.tsx                    # [MODIFY] 默认字段 + 变更确认
├── CollectionFieldsTab.tsx            # [MODIFY] 注册 secret 类型
├── SchemaFieldEditor.tsx              # [MODIFY] secret 字段渲染
├── CollectionUpdateConfirm.tsx        # [NEW] 变更确认面板
├── schema/
│   └── SecretFieldOptions.tsx         # [NEW] Secret 字段选项
│   └── SecretFieldOptions.test.tsx    # [NEW] 单元测试

webui/src/features/records/components/fields/
├── SecretField.tsx                    # [NEW] Secret 字段编辑组件
└── SecretField.test.tsx               # [NEW] 单元测试
```

## Architecture Overview

### 侧边栏布局变更

```
┌─────────────────────────────────────────────────────────────────┐
│                    CollectionsSidebar                            │
├─────────────────────────────────────────────────────────────────┤
│                                                                 │
│  ┌─────────────────────────────────────────────────────────────┐│
│  │                    Header (h-14)                            ││
│  │  ┌─────────────────────────────────────────────────────┐   ││
│  │  │ 🔍 Search...                                         │   ││
│  │  └─────────────────────────────────────────────────────┘   ││
│  └─────────────────────────────────────────────────────────────┘│
│                                                                 │
│  ┌─────────────────────────────────────────────────────────────┐│
│  │                    Content (flex-1, overflow-auto)          ││
│  │  • users                                                    ││
│  │  • posts                                                    ││
│  │  • ...                                                      ││
│  │  ▶ System (n)                                               ││
│  └─────────────────────────────────────────────────────────────┘│
│                                                                 │
│  ┌─────────────────────────────────────────────────────────────┐│
│  │                    Footer (NEW)                             ││
│  │  ┌─────────────────────────────────────────────────────┐   ││
│  │  │ [+] New collection                                   │   ││
│  │  └─────────────────────────────────────────────────────┘   ││
│  └─────────────────────────────────────────────────────────────┘│
│                                                                 │
└─────────────────────────────────────────────────────────────────┘
```

### Secret 字段数据流

```
┌─────────────┐     ┌─────────────┐     ┌─────────────┐
│ User Input  │────▶│ Encrypt     │────▶│ Database    │
│ "sk-xxx"    │     │ (AES-GCM)   │     │ (ciphertext)│
└─────────────┘     └─────────────┘     └─────────────┘
       │                                       │
       │                                       │
       ▼                                       ▼
┌─────────────┐     ┌─────────────┐     ┌─────────────┐
│ Display     │◀────│ Decrypt     │◀────│ API Read    │
│ (masked)    │     │ (AES-GCM)   │     │             │
└─────────────┘     └─────────────┘     └─────────────┘
```

### 变更确认流程

```
┌─────────────┐     ┌─────────────┐     ┌─────────────┐
│ Edit Panel  │────▶│ Diff Calc   │────▶│ Confirm     │
│ (formData)  │     │ (original   │     │ Panel       │
│             │     │  vs new)    │     │             │
└─────────────┘     └─────────────┘     └─────────────┘
                                               │
                           ┌───────────────────┼───────────────────┐
                           │                   │                   │
                           ▼                   ▼                   ▼
                    ┌─────────────┐     ┌─────────────┐     ┌─────────────┐
                    │ New Fields  │     │ Removed     │     │ Modified    │
                    │ List        │     │ Fields      │     │ Fields      │
                    └─────────────┘     └─────────────┘     └─────────────┘
```

## Key Design Decisions

### 1. 侧边栏新建按钮位置

**Decision**: 移动到侧边栏底部，独立于列表区域

**Rationale**:
- 与 UI (Svelte) 版本保持一致
- 始终可见，不受列表滚动影响
- 更符合用户期望的操作位置

**Trade-off**: 需要调整 Sidebar 组件布局

### 2. Secret 字段实现方式

**Decision**: 创建独立的 `SecretFieldOptions` 和 `SecretField` 组件

**Rationale**:
- 与其他字段类型保持一致的组件结构
- 便于单独测试和维护
- 复用现有的字段编辑器框架

### 3. 变更确认面板触发时机

**Decision**: 仅在编辑已有 Collection 时触发，新建时跳过

**Rationale**:
- 新建 Collection 无需确认（无破坏性操作）
- 编辑时需要警告字段删除等危险操作
- 与 UI (Svelte) 版本行为一致

### 4. 默认字段添加时机

**Decision**: 在 UpsertPanel 打开时（新建模式）立即添加

**Rationale**:
- 用户可以看到并决定是否保留
- 可以修改或删除默认字段
- 与 UI (Svelte) 版本行为一致

## Component Specifications

### SecretFieldOptions.tsx

```tsx
interface SecretFieldOptionsProps {
  field: SchemaField
  onChange: (field: SchemaField) => void
}

// 功能：
// - maxSize 输入 (默认 4096)
// - 帮助文本 "Default to ~4KB"
```

### SecretField.tsx (Record 编辑)

```tsx
interface SecretFieldProps {
  value: string
  onChange: (value: string) => void
  disabled?: boolean
}

// 功能：
// - 密码类型输入框
// - 显示掩码 "sk-••••••••••345"
// - Reveal 按钮切换显示/隐藏
```

### CollectionUpdateConfirm.tsx

```tsx
interface CollectionUpdateConfirmProps {
  open: boolean
  onClose: () => void
  onConfirm: () => void
  original: CollectionModel
  updated: CollectionModel
}

// 功能：
// - 计算字段变更 (新增/删除/修改)
// - 显示变更摘要
// - 警告破坏性操作
// - 确认/取消按钮
```

## Testing Strategy

### TDD 流程

```
┌─────────────────────────────────────────────────────────────────┐
│  Step 1: 创建测试文件                                            │
│  ├── 创建 xxx.test.tsx                                          │
│  ├── 编写测试用例（描述期望行为）                                  │
│  └── 运行测试 → 确认失败（红灯 🔴）                               │
│                                                                 │
│  Step 2: 实现代码                                                │
│  ├── 编写最小实现使测试通过                                       │
│  └── 运行测试 → 确认通过（绿灯 🟢）                               │
│                                                                 │
│  Step 3: 重构                                                    │
│  ├── 优化代码结构                                                │
│  └── 运行测试 → 保持通过（绿灯 🟢）                               │
└─────────────────────────────────────────────────────────────────┘
```

### 测试覆盖范围

| 组件 | 测试类型 | 覆盖率目标 |
|------|---------|-----------|
| SecretFieldOptions | 单元测试 | ≥ 80% |
| SecretField | 单元测试 | ≥ 80% |
| CollectionUpdateConfirm | 单元测试 | ≥ 80% |
| Sidebar (新建按钮) | 集成测试 | ≥ 80% |
| UpsertPanel (默认字段) | 集成测试 | ≥ 80% |

### 测试豁免

根据项目规范，以下不需要单元测试：
- UI 组件（纯展示，无逻辑）
- Types 类型文件

## Risk Assessment

| Risk | Probability | Impact | Mitigation |
|------|-------------|--------|------------|
| Secret 字段后端不兼容 | Low | High | 先验证 API 响应格式 |
| 变更计算逻辑复杂 | Medium | Medium | 参考 Svelte 实现 |
| 样式与设计规范不一致 | Low | Low | 遵循 Apple-style 规范 |
| 测试覆盖率不达标 | Low | Medium | TDD 流程保证 |

## Success Metrics

| Metric | Target | Current |
|--------|--------|---------|
| 功能对齐度 | 100% | - |
| 测试覆盖率 | ≥ 80% | - |
| 无回归问题 | 0 | - |
| 代码审查通过 | Yes | - |

## Dependencies

### Internal

| Component | Purpose |
|-----------|---------|
| `core/field_secret.go` | Secret 字段后端实现 |
| `CollectionFieldsTab.tsx` | 字段列表容器 |
| `SchemaFieldEditor.tsx` | 字段编辑器 |
| `UpsertPanel.tsx` | Collection 编辑面板 |

### External

| Package | Version | Purpose |
|---------|---------|---------|
| `lucide-react` | ^0.x | KeyRound 图标 |
| `shadcn/ui` | - | UI 组件 |
| `vitest` | ^1.x | 测试框架 |
| `@testing-library/react` | ^14.x | 组件测试 |

## Timeline Estimate

| Phase | Tasks | Est. Hours |
|-------|-------|------------|
| Phase 1: 侧边栏改造 | 2 | 2h |
| Phase 2: Secret 字段 | 4 | 4h |
| Phase 3: 默认字段 | 2 | 1h |
| Phase 4: 变更确认 | 4 | 4h |
| Phase 5: 测试补充 | 4 | 3h |
| **Total** | **16** | **~14h** |
